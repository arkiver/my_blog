defaults: &defaults
  adapter: sqlite

development:
  database: db/my_blog_development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: db/my_blog_test.db
  <<: *defaults
production:
  database: db/my_blog_production.db
  <<: *defaults
